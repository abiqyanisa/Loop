#include <iostream>
#include <ncurses.h>
#include <windows.h>

using namespace std;

//#define baris_a 10
//#define kolom_a 15

//#define baris_b 20
//#define kolom_b 30

int main(){
	
	initscr;
	
	//short int baris_a, kolom_a, baris_b, kolom_b;
	
	//mvprintw(baris_a, kolom_a, " A ");
	
	for(int n=1; n<12; n++){
		
		for(int i=1; i<5; i++){
			
			mvprintw(5, 2+n, "A");
			refresh();
			Sleep(100);
	
		}
		
		mvprintw(4,2+n, " ");
		refresh();
		Sleep(300);
	}
	
}
